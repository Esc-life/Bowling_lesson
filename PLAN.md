# 3D 볼링 게임 + 초등 교육용 튜토리얼 구현 계획

## Context

사용자는 3D 게임을 만들고 싶고, 볼링/야구 중 **더 빨리 구현 가능한 종목**을 원했다. 볼링을 선택한다: 조작이 "공 굴리기" 하나로 끝나고, 재미의 핵심을 물리엔진이 대신 해주며, AI 상대와 복잡한 상태머신(이닝/아웃/주루)이 필요 없다. 야구는 투구 구질 + 배트-공 충돌 판정 + 야수 AI + 주루까지 필수라 MVP 도달 시간이 수 배로 늘어난다.

추가로 **초등 고학년(10~12세) 교육용**으로 쓸 수 있어야 한다. 볼링 규칙과 기본 자세·투구법을 영역별로 나눈 튜토리얼로 가르친다.

`C:\Users\user\Desktop\test`는 빈 디렉터리이므로 그린필드 프로젝트다.

**목표 결과물**
1. 브라우저에서 마우스 드래그로 공을 던져 10프레임을 완주하고 정식 볼링 점수가 계산되는 게임
2. 5개 영역 18개 레슨의 튜토리얼 — 그림 + 짧은 설명 + 4지선다 퀴즈 + 게임 내 실습 드릴, 진행률 저장

**확정된 방향** (사용자 선택)

| 항목 | 결정 |
|---|---|
| 스택 | Vite + TypeScript + Three.js(렌더) + Rapier(물리), 웹 |
| 조작 | 마우스 드래그 스윙 (방향 + 파워 + 스핀을 드래그 하나로) |
| 게임 MVP | 1인 10프레임 완주 + 정식 점수 계산 |
| 비주얼 | 기본 도형 + 코드로 생성하는 절차적 텍스처 (외부 3D 에셋 없음) |
| **대상** | **초등 고학년 (10~12세)** |
| **투구 손** | **좌/우 모두 지원 + 첫 실행 시 선택 화면** |
| **퀴즈** | **4지선다 (오답 보기는 흔한 오해에서 자동 생성)** |
| **결과 확인** | **화면 리포트만 (내보내기 없음)** |

### 교육용으로서의 정직한 범위

| 가르칠 수 있는 것 (게임 내 실습·판정 가능) | 전달만 가능한 것 (다이어그램·설명) |
|---|---|
| 규칙, 프레임 구조, 점수 계산 | 실제 몸의 자세, 걸음 리듬, 공 잡는 법 |
| 핀 번호·포켓 찾기 | 공을 놓는 순간의 손목 동작 |
| 화살표 보고 조준하기 | 내 손에 맞는 공 무게 고르기 |
| 공이 휘는 이유(오일과 회전의 관계) | |

**마우스 드래그는 실제 스윙과 물리적으로 다르다.** 자세 영역(C)은 "게임을 잘하는 법"이 아니라 "실제 볼링장에서의 지식"으로 분리하고, 각 레슨 상단에 그 사실을 배너로 고정 표시한다. 실제 폼은 화면으로 교정할 수 없다고 튜토리얼 안에 명시한다. 이 구분을 흐리면 교육 자료로서 신뢰를 잃는다.

---

## 아키텍처

**설계 원칙**
1. 규칙(점수/프레임)은 Three.js와 Rapier를 전혀 모르는 순수 TypeScript로 격리한다 → 유닛 테스트 가능, 물리 튜닝 중에도 규칙이 흔들리지 않는다.
2. 튜토리얼 **콘텐츠는 데이터**, 엔진은 코드다. 레슨을 추가·수정할 때 `curriculum.ts`만 고치고 게임 코드는 건드리지 않는다.
3. **좌/우 투구는 처음부터 전제한다.** 나중에 끼워 넣으면 드릴 목표·다이어그램·판정을 전부 고쳐야 한다.

```
Desktop/test/
  package.json, tsconfig.json, vite.config.ts, index.html
  PLAN.md                     이 계획 문서
  src/
    main.ts                   부트스트랩 + requestAnimationFrame 루프
    config.ts                 ★ 모든 물리·게임플레이 상수 한 곳에 집중
    settings.ts               ★ handedness 등 사용자 설정 + localStorage
    core/
      Game.ts                 루프 오케스트레이션, 서브시스템 소유
      Input.ts                포인터 드래그 캡처 → ThrowInput {speed, angle, spin, startX}
      Mode.ts                 free-play / tutorial 모드 전환
    physics/
      World.ts                Rapier 초기화, 고정 timestep 스텝
      Bodies.ts               공/핀/레인 강체 생성 + 풀링 리셋 (임의 핀 부분집합 배치)
      PinState.ts             핀 넘어짐 판정, 정지(settle) 감지
    scene/
      SceneSetup.ts           renderer, 조명, 톤매핑, 리사이즈
      Lane.ts / Pin.ts / Ball.ts
      Textures.ts             Canvas2D → CanvasTexture (나뭇결, 핀 스트라이프, 화살표)
      canvas2d.ts             ★ Canvas2D 드로잉 헬퍼 (튜토리얼 다이어그램과 공용)
      CameraRig.ts            aim / follow / pindeck / result / topdown
      AimGuide.ts             드래그 중 조준선
      Trajectory.ts           공 궤적 리본 (관찰 모드 / 훅 레슨용)
    rules/
      Scorecard.ts            ★ 순수: 10프레임 점수 계산
      Scorecard.test.ts
      FrameMachine.ts         ★ 순수: 턴/볼/프레임 진행 상태머신
      pinLayout.ts            ★ 순수: 핀 번호↔좌표, 포켓, 좌우 미러링
      pinLayout.test.ts
    tutorial/
      types.ts                Area, Lesson, Quiz, Drill, Glossary 타입
      curriculum.ts           ★ 콘텐츠 데이터 (영역 A~E, 18개 레슨)
      glossary.ts             ★ 용어집 데이터 (본문 용어 클릭 → 팝업)
      TutorialFlow.ts         ★ 순수: 현재 위치, 완료 판정, 진행률
      TutorialFlow.test.ts
      Progress.ts             localStorage 저장/복원/초기화
      QuizRunner.ts           ★ 순수: 4지선다 채점 + 오답별 피드백
      QuizRunner.test.ts
      distractors.ts          ★ 순수: 점수 문항의 오답 보기 자동 생성
      DrillRunner.ts          드릴 셋업(핀 배치) + 성공 조건 평가
      diagrams/
        PinDeckDiagram.ts     핀 번호·포켓 (좌우 미러 지원)
        LaneTopDiagram.ts     화살표·도트·오일 패턴 상면도 (좌우 미러 지원)
        ScorecardDiagram.ts   점수판 예시 (Scoreboard 렌더러 재사용)
        ApproachDiagram.ts    4걸음 측면도 관절 애니메이션
    ui/
      Scoreboard.ts           DOM 오버레이 스코어카드 (읽기전용·빈칸 모드 겸용)
      Hud.ts                  파워 게이지, "스트라이크!" 메시지, 재시작
      HandPicker.ts           ★ 첫 실행 시 왼손/오른손 선택 화면
      TutorialPanel.ts        레슨 본문 패널 (그림 + 짧은 글 + 퀴즈)
      GlossaryPopover.ts      용어 팝업
      AreaMenu.ts             영역 선택 화면 + 진행률 + 초기화 버튼
      ReportScreen.ts         수료 리포트 (화면 표시만)
      ObserveControls.ts      슬로모션·일시정지·궤적 토글
```

의존성 방향: `ui`/`scene`/`physics`/`tutorial` → `rules` (역방향 금지). `rules`와 `tutorial`의 Flow/Quiz/distractors는 순수 로직.

---

## 게임 핵심 설계 결정

### 1. 단위와 치수 — 실제 볼링 규격을 미터로 (`config.ts`)

현실 치수를 쓰면 물리 튜닝이 "그럴듯한 값 찾기"가 아니라 "현실과 비교하기"가 된다. **교육용이므로 더 중요하다** — 튜토리얼이 인용하는 수치와 게임 물리가 같은 출처를 쓴다.

| 항목 | 값 | 초등용 비유 (튜토리얼에서 사용) |
|---|---|---|
| 파울라인 → 1번핀 | 18.29 m (60 ft) | 버스 두 대를 세워 놓은 길이 |
| 레인 폭 | 1.0566 m = 나무판 39장 | 두 팔을 벌린 정도 |
| 공 | 반지름 0.10875 m, 질량 6.8 kg | 축구공보다 조금 작고 훨씬 무겁다 |
| 핀 | 높이 0.381 m, 질량 1.53 kg | 2리터 물병 정도 |
| 핀 간격 | 중심간 0.3048 m (12 in) | 30 cm 자 하나 |
| 화살표 | 파울라인에서 약 3.7~4.6 m, 7개 | |
| 릴리즈 속도 | 5.0 ~ 9.0 m/s | |

좌표계: **+Z가 레인 진행 방향**, +Y가 위, X가 좌우(중앙 0).

```
핀 배치 (pinLayout.ts — 번호↔좌표를 단일 출처로)
1번:  (0,       18.290)
2,3:  (±0.1524, 18.554)
4,5,6:(0, ±0.3048, 18.818)
7~10: (±0.1524, ±0.4572, 19.082)
```

### 2. 좌/우 투구 지원 (`pinLayout.ts` + `settings.ts`)

**물리는 미러링하지 않는다.** 공 시작 위치와 회전 방향은 학생의 입력이므로 좌우 대칭이 이미 자연스럽게 성립한다. 미러링이 필요한 것은 **판정과 교육 콘텐츠**뿐이다.

```ts
type Handedness = 'right' | 'left'

pocketPins(h)   // right → [1,3],  left → [1,2]
mirrorPin(n)    // 7↔10, 4↔6, 2↔3, 8↔9, 1·5 고정
mirrorPins(ns)
targetArrow(h)  // 2번 화살표: right → 오른쪽에서 2번째, left → 왼쪽에서 2번째
```

- 드릴 셋업의 `standingPins`와 목표는 오른손 기준으로 작성하고, 왼손이면 `mirrorPins()`를 통과시킨다. 커리큘럼을 두 벌 쓰지 않는다.
- 다이어그램은 Canvas2D `transform`으로 X축을 한 번 뒤집는다(`ctx.scale(-1,1)`), 글자만 되돌린다.
- `mirrorPin`은 자기역함수여야 한다(`mirrorPin(mirrorPin(n)) === n`) → 유닛 테스트.

첫 실행 시 `HandPicker`가 큰 버튼 두 개로 묻는다("공을 어느 손으로 던지나요?"). 그림(왼손/오른손 아이콘)을 함께 넣어 글을 덜 읽게 한다. `settings.ts`에 저장하고 설정에서 언제든 바꿀 수 있다. 공용 PC에서 학생이 바뀔 때 자연스럽게 재설정되는 이점도 있다.

### 3. 핀 콜라이더는 원기둥 3개 복합체

핀이 넘어지는 모양새는 **무게중심 높이**가 결정한다. 단일 캡슐로 만들면 핀이 팽이처럼 굴러 어색하다.

- 시각: `LatheGeometry`로 핀 실루엣 회전체 (프로파일 점 8개)
- 물리: 하나의 RigidBody에 자식 콜라이더 3개 — 하부(굽) / 몸통(벨리) / 목+머리
- 하부 콜라이더 density를 높여 실제 핀처럼 **아래가 무겁게** 만든다. Rapier가 복합 콜라이더에서 질량·관성 텐서를 자동 계산한다.
- 볼록 껍질(convexHull)은 쓰지 않는다 — 핀의 오목한 목 부분이 사라진다.

### 4. 물리 스텝: 고정 1/120초 + CCD

핀은 얇고 길며 공은 18 m를 8 m/s로 달린다 → 기본 1/60초면 터널링·불안정이 발생한다.

- `world.timestep = 1/120`, 누산기(accumulator)로 프레임레이트와 분리
- 렌더링은 두 물리 스텝 사이를 보간
- 공에 `setCcdEnabled(true)`
- 프레임당 최대 스텝 수 제한(8)으로 탭 복귀 시 스파이럴 방지
- **슬로모션은 timestep을 줄이지 않고 스텝 횟수를 줄여서** 구현한다 (물리 정확도 유지)

### 5. 후크(훅) 구현: 레인 오일 패턴을 마찰 2구역으로

실제 볼링에서 공이 휘는 이유는 레인 앞 2/3에만 오일이 있기 때문이다. 이걸 구현하면 회전이 실제로 의미를 갖고, **레슨 D3(공이 왜 휘나요?)를 눈으로 보여줄 수 있다.**

- 레인 표면 콜라이더를 Z 방향으로 2~3개로 분할
  - 오일 구간 (0 ~ 12.2 m): friction ≈ 0.04 → 공이 회전을 유지하며 미끄러진다
  - 드라이 구간 (12.2 m ~): friction ≈ 0.20 → 회전이 접지력으로 바뀌며 휘어진다
- 마찰 결합 규칙 `CoefficientCombineRule.Multiply`
- 회전은 릴리즈 시 Y축 각속도 (드래그 마지막 순간의 좌우 속도에서 산출)
- 오일 구간을 반투명 오버레이로 시각화하는 토글 → 그대로 수업 자료

### 6. 드래그 조작 매핑 (`Input.ts`)

포인터 이벤트를 시간 스탬프와 함께 링 버퍼(최근 ~120 ms)에 저장한다. **Pointer Events를 쓰므로 터치패드·태블릿·펜이 자동 지원된다** — 교실 환경에서 중요한 부수효과다.

1. **드래그 전**: 좌우 드래그(또는 A/D)로 파울라인상의 시작 위치 `startX` 조정 (±0.4 m)
2. **pointerdown → 화면 아래로 당기기**: 당긴 길이 → 파워, 좌우 성분 → 발사 각도(±8° 클램프)
3. **pointerup 직전 좌우 속도** → 회전(Y축 각속도, ±30 rad/s 클램프)
4. 드래그 중 `AimGuide`가 방향선을 그리고 파워에 따라 색이 변한다

**초등 대상이므로 클램프 범위를 좁게 시작한다.** 실수해도 공이 거터로 빠지는 빈도를 낮춘다. `config.ts`에 난이도 상수를 두고, 자유 연습에서 "쉽게/어렵게"를 고를 수 있게 한다.

`Input`은 Three.js/Rapier를 모르는 순수 `ThrowInput`만 내보낸다 → 나중에 게이지 방식 입력을 추가해도 게임 코드는 그대로다.

### 7. 정지 감지와 핀 판정 (`PinState.ts`)

- **넘어짐 판정**: 핀의 로컬 up 벡터와 월드 up의 내적 < cos(45°), 또는 원위치에서 0.1 m 이상 이동, 또는 Y가 핀덱 아래로 낙하
- **정지 판정**: (공이 핀덱을 지났거나 핏에 빠짐) AND (모든 강체 속도가 임계값 이하로 0.5초 연속) → 또는 하드 타임아웃 7초
- 판정은 정지 후 **한 번만** 수행하고, 결과를 `FrameMachine`과 `DrillRunner`에 함께 넘긴다

### 8. 규칙 계층 (순수 로직 + 테스트)

`Scorecard.ts` — 굴림 배열 하나를 진실의 원천으로 두고 프레임 점수를 누적 계산한다.

```ts
class Scorecard {
  roll(pinsKnocked: number): void
  get frames(): FrameView[]      // rolls, frameScore|null, cumulative|null
  get currentFrame(): number     // 1..10
  get ballInFrame(): number      // 1..3 (10프레임)
  get pinsStanding(): number
  get isGameOver(): boolean
  get total(): number
}
```

`FrameMachine.ts` — 상태 전이:
```
AIMING → THROWN → SETTLING → RESOLVING
   → (같은 프레임 2구: 넘어진 핀만 제거, 서 있는 핀 유지)
   → (새 프레임/스트라이크/10프레임 보너스: 10핀 전체 재배치)
   → ... → GAME_OVER
```

`Bodies.ts`의 **`setPins(standing: boolean[])`** 이 핵심 재사용 지점이다. 스페어 상황을 위해 원래 필요한 함수이고, 튜토리얼 드릴("7번 핀만 세우고 처리하기")이 이것을 그대로 쓴다. 핀 바디는 풀링해 재사용한다(생성/파괴 없이 `setTranslation`/`setRotation`/속도 0 + `wakeUp()`).

### 9. 비주얼 / 카메라

`Textures.ts`에서 오프스크린 `<canvas>`에 그려 `THREE.CanvasTexture`로 변환: 레인 나뭇결·파울라인·화살표 7개(교육상 정확한 위치), 핀 흰 바탕 + 빨간 링 2줄 + 핀 번호 표시 토글, 공 단색 + 손가락 구멍 3개(어두운 원, CSG 불필요).

조명: 반구광 + 방향광 1개(그림자) + 어두운 배경. `ACESFilmicToneMapping`, `PCFSoftShadowMap`, `setPixelRatio(min(dpr, 2))`.

**Canvas2D 드로잉 헬퍼를 `scene/canvas2d.ts`로 분리한다** — 튜토리얼 다이어그램 4종이 같은 헬퍼를 재사용한다.

카메라: `aim`(공 뒤 1.6 m) / `follow` / `pindeck`(Z=15 m 통과 시 컷) / `result` / `topdown`(조준·오일 패턴 설명용). 이름 붙은 모드 + 위치·쿼터니언 lerp 전환.

---

## 튜토리얼 설계

### 초등 고학년용 글쓰기 규칙 (18개 레슨 전체에 적용)

이 규칙을 먼저 정해 두지 않으면 레슨마다 톤이 달라진다.

- **한 문장 = 한 줄**, 25자 안쪽을 지향
- **레슨 본문 150자 이내**, 불릿 3개 이내
- **그림이 먼저, 글이 나중** — 모든 레슨에 다이어그램 또는 3D 장면 필수
- **어려운 한자어 금지**: 전도→넘어짐, 기립→서 있는, 침범→밟기, 정렬→나란히, 명중→맞히기
- **영문 병기 최소화** — 초등에게는 오히려 방해다. 이미 외래어로 굳은 말(스트라이크, 스페어)은 그대로 쓴다
- **큰 숫자는 비유를 붙인다** (위 치수 표 참조)
- 친근한 존댓말 ("한번 던져 볼까요?")
- 어려운 낱말은 본문에서 **밑줄 → 클릭하면 용어집 팝업**. 별도 용어 암기 레슨을 만들지 않는다

### 데이터 모델 (`tutorial/types.ts`)

```ts
type Area = {
  id: 'A' | 'B' | 'C' | 'D' | 'E'
  title: string; summary: string
  lessons: Lesson[]
  recommendedAfter?: Area['id'][]   // 권장 순서만, 강제 잠금 아님
}

type Lesson = {
  id: string                        // 'B3' — 딥링크 키
  title: string
  body: Block[]                     // 짧은 문단 / 불릿 / 강조 / 용어 링크
  diagram?: DiagramSpec             // 어떤 다이어그램을 어떤 인자로 그릴지
  check?: Quiz | Drill              // 완료 조건 (없으면 '읽음'으로 완료)
  estimatedMinutes: number          // 교사가 수업 시간을 배분할 수 있게
}

type Quiz = { kind: 'quiz'; questions: Question[]; passRatio: number }
type Question =
  | { kind: 'choice'; prompt: string; choices: Choice[]; }
  | { kind: 'scoreChoice'; prompt: string; rolls: number[]; askFrame: number }
                                     // 보기와 정답을 실행 시점에 생성 (아래 참조)
type Choice = { text: string; correct: boolean; feedback?: string }

type Drill = {
  kind: 'drill'
  setup: { standingPins: number[]; startX?: number; hint?: string }  // 오른손 기준
  goal: DrillGoal
  attempts: number                  // 실패해도 진행 가능
  assist?: boolean                  // '도움 받기' 허용 (조준 보조)
}
```

`DrillGoal`은 **`PinState`와 `Scorecard`의 결과만으로 판정 가능한 술어**로 제한한다. 드릴마다 새 판정 코드를 쓰기 시작하면 유지보수가 무너진다.
`knockAll` / `knockPins: number[]` / `pocketHit` / `strike` / `spare` / `minScore: n`

### 4지선다 — 오답 보기를 흔한 오해에서 자동 생성 (`distractors.ts`)

점수 계산 문항(`scoreChoice`)은 정답도 오답도 하드코딩하지 않는다.

- **정답**: `Scorecard`에 `rolls`를 넣어 계산 → 규칙 구현과 교육 내용이 어긋날 수 없다
- **오답**: 초등 학생이 실제로 하는 오해를 규칙으로 재현한다
  1. 보너스를 빼먹고 그냥 `10`
  2. 스트라이크인데 다음 **1구**만 더함 (스페어 계산법과 혼동)
  3. 프레임 점수 자리에 **누적 점수**를 적음 (또는 그 반대)
  4. 쓰러진 핀 수만 단순 합산
- 각 오답에 **왜 틀렸는지 한 줄 피드백**을 붙인다 → 학생이 그 보기를 고르면 바로 표시. "그건 스페어를 계산하는 방법이에요. 스트라이크는 다음 **두 번**을 더해요."
- 생성된 4개 보기는 중복 제거 후 셔플하고, 값이 겹쳐 4개가 안 되면 근사값으로 채운다. 셔플은 시드를 고정해 같은 문항이 매번 같은 순서로 나오게 한다(교사가 화면을 미리 확인할 수 있어야 한다).

이 방식은 문항 추가 비용이 `rolls` 배열 하나뿐이다. B영역 문항을 늘려도 손계산이 필요 없다.

### 커리큘럼 (`tutorial/curriculum.ts`) — 5개 영역 18개 레슨, 총 약 50분

영역은 독립 진입 가능하다. **강제 잠금은 두지 않고 권장 순서만 표시한다** — 교사가 필요한 영역만 골라 쓸 수 있어야 한다.

**영역 A — 볼링장 둘러보기** (3개, 약 7분)
| 레슨 | 내용 | 완료 조건 |
|---|---|---|
| A1 | 레인 구조: 서는 곳, 넘으면 안 되는 선, 양옆 홈, 핀이 서는 곳. 길이 비유 | 읽음 |
| A2 | 핀 번호(1~10)와 포켓 — 어디를 맞히면 잘 쓰러지나 | 퀴즈 3문항 (다이어그램에서 번호 찾기) |
| A3 | 공 고르기와 잡기: 무게는 몸무게의 1/10쯤, 구멍 세 개에 손가락 넣기 | 읽음 |

**영역 B — 규칙과 점수** (5개, 약 15분) — *교육 가치가 가장 확실한 영역*
| 레슨 | 내용 | 완료 조건 |
|---|---|---|
| B1 | 10번의 프레임, 프레임마다 두 번의 기회 | 퀴즈 2문항 |
| B2 | 스페어: 두 번에 다 쓰러뜨리면 10 + **다음 한 번** | `scoreChoice` 3문항 |
| B3 | 스트라이크: 한 번에 다 쓰러뜨리면 10 + **다음 두 번** | `scoreChoice` 3문항 |
| B4 | 마지막 10프레임은 특별하다 (최대 세 번) + 300점 만점 이야기 | `scoreChoice` 2문항 |
| B5 | 반칙: 선을 밟으면 그 번은 0점. 홈에 빠지면 0개 | 퀴즈 2문항 |

점수판 문항은 `ui/Scoreboard.ts`를 **읽기 전용 + 빈칸 하이라이트 모드**로 재사용한다. 학생은 표를 보고 보기 중에서 고른다.

**영역 C — 자세와 던지는 방법** (4개, 약 10분) — *실제 볼링장 지식 파트로 명시*
| 레슨 | 내용 | 완료 조건 |
|---|---|---|
| C1 | 준비 자세: 어디에 서고, 공은 어디에 들고, 어디를 보는지 | 읽음 |
| C2 | 네 걸음으로 걸어 나가기 (밀기 → 내리기 → 뒤로 → 앞으로 → 놓기) | 애니메이션 관찰 |
| C3 | 공을 놓는 순간과 팔 뻗기 | 읽음 |
| C4 | 자주 하는 실수 네 가지와 고치는 법 | 퀴즈 3문항 |

각 레슨 상단에 **"이건 진짜 볼링장에서 하는 방법이에요. 마우스로 던지는 것과는 달라요"** 배너를 고정한다.

`ApproachDiagram.ts` — 측면도 관절 스틱 피겨. 5개 키프레임의 관절 각도(어깨·팔꿈치·엉덩이·무릎·발목)를 `curriculum.ts`에 데이터로 두고 보간 재생 + 슬라이더로 스크럽. 3D 캐릭터 리깅은 하지 않는다 — 비용 대비 교육 효과가 낮다.

**영역 D — 조준하고 맞히기** (4개, 약 12분) — *게임에서 진짜로 연습 가능한 영역*
| 레슨 | 내용 | 완료 조건 |
|---|---|---|
| D1 | 바닥의 화살표를 보고 조준하기 (핀이 아니라 화살표를 본다) | 드릴: 포켓 1회 (시도 5회, 도움 가능) |
| D2 | 포켓을 맞히면 스트라이크가 잘 나온다 | 드릴: 스트라이크 1회 (시도 8회, 도움 가능) |
| D3 | 공은 왜 휘나요? — 앞쪽은 기름, 뒤쪽은 마른 바닥 | 궤적 3회 관찰 |
| D4 | 남은 핀 처리하기: 남은 핀 쪽으로 조금 옮겨 서기 | 드릴: 7번·10번 핀 각각 처리 |

D3는 `Trajectory.ts` 궤적 리본 + 오일 구간 오버레이 + 슬로모션을 함께 켜서, **회전을 주지 않은 궤적과 준 궤적을 겹쳐 보여준다.** 물리엔진이 이미 계산하는 것을 시각화만 하므로 저렴하고 설득력이 높다.

*초등 수준을 넘는 3-6-9 스페어 시스템과 스플릿 유형 분류는 넣지 않는다.* D4는 "남은 핀 쪽으로 옮겨 선다"는 단순 규칙으로 대체한다.

**영역 E — 실제로 해 보기** (2개, 약 8분)
| 레슨 | 내용 | 완료 조건 |
|---|---|---|
| E1 | 도전: 포켓 3번 맞히기 | 드릴: 포켓 3회 (시도 12회) |
| E2 | 10프레임 끝까지 던지고 점수 확인하기 | 드릴: 완주 (`minScore` 40점, 도움 가능) |

E2 완료 후 `ReportScreen`이 영역별 진행률·퀴즈 정답률·드릴 성공 여부를 화면에 보여준다. **내보내기·인쇄 기능은 만들지 않는다.**

### 진행 관리 (`TutorialFlow.ts` + `Progress.ts`)

- `TutorialFlow`는 순수 로직: `{completedLessons, quizScores, currentLesson}` 상태에서 다음 레슨과 영역별 진행률(%)을 계산. 유닛 테스트 대상.
- `Progress`는 localStorage 어댑터. **키에 스키마 버전을 포함**해 커리큘럼이 바뀌어도 옛 저장 데이터가 깨지지 않게 한다(모르는 레슨 ID는 무시).
- **공용 PC 대응**: 진행률 초기화 버튼을 `AreaMenu`에 명시적으로 배치. `HandPicker`도 다시 뜬다.

### 교사용 기능 (저렴하고 효과가 큼)

- **딥링크** `?lesson=B3` / `?area=D` — 수업 중 특정 지점을 바로 띄운다. 라우팅 프레임워크 없이 쿼리스트링 한 줄.
- **관찰 모드** (`ObserveControls.ts`): 슬로모션(1/4, 1/8), 일시정지, 궤적 표시, 오일 구간 오버레이, 핀 번호 표시. 원래 개발용 디버그 오버레이를 교육 도구로 승격시킨 것이므로 추가 비용이 거의 없다.
- **자유 연습에서 임의 핀 배치 지정** — 교사가 원하는 상황을 즉석에서 시연

---

## 구현 순서 (각 단계마다 실행 가능한 결과물)

| 단계 | 내용 | 확인 방법 |
|---|---|---|
| **M0** | Vite+TS 스캐폴딩, Three 씬, 회색 레인 박스, 고정 카메라, 리사이즈 | `npm run dev` → 레인이 보인다 |
| **M1** | Rapier 초기화, 고정 스텝, 공 1개 + 핀 10개 복합 바디, 키 입력 임펄스 | 공이 굴러가 핀이 넘어진다 |
| **M2** | 드래그 입력 + 조준선 + 파워/각도/회전 매핑, 오일 구간 마찰 | 드래그로 휘는 공을 던진다 |
| **M3** | `pinLayout`(**미러링 포함**) + `settings.ts` + 정지 감지 + `setPins()` + `FrameMachine` | 프레임이 넘어가고 스페어 상황이 정확하다 |
| **M4** | `Scorecard` + Vitest + DOM 스코어보드 | `npm test` 통과, 화면에 점수판 |
| **M5** | 절차적 텍스처, 조명·그림자, 카메라 모드 전환 | 볼링장처럼 보인다 |
| **M6** | 관찰 모드: 슬로모션, 궤적 리본, 오일 오버레이, 핀 번호 | 공이 왜 휘는지 눈으로 보인다 |
| **M7** | 튜토리얼 골격: 타입, `TutorialFlow`(+테스트), `Progress`, `TutorialPanel`, `AreaMenu`, `HandPicker`, 딥링크 — 콘텐츠는 A1/B3/D2 3개만 | 레슨 3개를 끝까지 진행하고 새로고침 후 이어서 할 수 있다 |
| **M8** | `distractors` + `QuizRunner`(+테스트) + `DrillRunner` + 다이어그램 4종 + 용어집 팝업 | 4지선다 채점·오답 피드백·드릴 판정이 동작한다 |
| **M9** | 커리큘럼 콘텐츠 전체 (영역 A~E, 18개 레슨) + `ReportScreen` | 처음부터 끝까지 수료 가능 |
| **M10** | 폴리시: 스트라이크 연출, 격려 메시지, 큰 글씨·버튼, 난이도 튜닝 | 초등학생이 혼자서도 진행할 수 있다 |

M0~M4가 게임의 뼈대, M5~M6이 체감 품질 겸 교육 도구, M7~M9가 튜토리얼이다.

**두 가지 순서상 중요한 점**
- **M3에 좌/우 미러링을 넣는다.** 나중에 넣으면 드릴 목표와 다이어그램을 전부 고쳐야 한다.
- **M7은 골격만 만들고 콘텐츠는 3개만 넣는다.** 18개를 다 쓴 뒤에 골격 결함을 발견하면 전부 고쳐야 한다.

---

## 검증

### 유닛 테스트 (`npm test`, Vitest)

`rules/Scorecard.test.ts`
- 거터 게임 → 0점 / 퍼펙트 게임(12연속 스트라이크) → 300점 / 전 프레임 5-5 스페어 → 150점
- 알려진 혼합 게임 → 손계산 값과 일치
- 10프레임 엣지 케이스: 스트라이크 후 2보너스, 스페어 후 1보너스, 오픈 종료, X-X-X
- 보너스 미확정 프레임의 누적 점수가 `null`로 유지되는지
- 잘못된 입력 방어: 한 프레임 2구 합 > 10, 게임 종료 후 `roll()`

`rules/pinLayout.test.ts`
- `mirrorPin`이 자기역함수 (`mirrorPin(mirrorPin(n)) === n`, 1~10 전부)
- 미러 쌍 정확성: 7↔10, 4↔6, 2↔3, 8↔9, 1·5 고정
- `pocketPins('right') === [1,3]`, `pocketPins('left') === [1,2]`
- 미러링된 핀 좌표의 X가 원본의 부호 반전과 일치

`tutorial/QuizRunner.test.ts` + `distractors.test.ts`
- `scoreChoice` 정답이 `Scorecard` 계산과 일치 (정답 하드코딩이 없음을 확인)
- 오답 4종이 정답과 겹치지 않고, 보기가 항상 4개
- 오답 값이 우연히 정답과 같아지는 `rolls` 케이스에서도 4개를 채우는지
- 셔플이 시드 고정으로 재현되는지
- 채점, `passRatio` 경계값, 오답별 피드백이 올바른 보기에 붙는지

`tutorial/TutorialFlow.test.ts`
- 레슨 완료 → 다음 레슨 이동, 영역 진행률 계산
- **커리큘럼 정합성 테스트**: 레슨 ID 유일, `recommendedAfter`가 존재하는 영역만 참조, 모든 `standingPins`가 1~10, `DrillGoal.knockPins`가 `standingPins`의 부분집합, 모든 레슨에 `diagram` 또는 3D 장면이 있는지, **본문 길이가 150자를 넘지 않는지** → 18개를 눈으로 검수하지 않아도 오타와 규칙 위반을 기계가 잡는다
- 저장 데이터에 모르는 레슨 ID가 있어도 크래시하지 않는지 (스키마 버전 마이그레이션)

### 브라우저 수동 검증 (`npm run dev`)

게임:
1. `?debug=1`에서 숫자 키로 핀 수를 강제 입력해 10프레임을 물리 없이 통과 → 점수판 정확성
2. 스트라이크 시 10핀 전체 재배치, 1구 후 **서 있던 핀만 남는지**
3. 회전 없는 직구가 헤드핀에 직진하는지, 강한 회전이 뒷구간에서 휘는지
4. 탭 전환 후 복귀 시 물리 폭주 없음, 최대 파워에서 공이 관통하지 않음 (CCD)
5. 창 리사이즈 / 다양한 해상도

좌/우 및 튜토리얼:
6. 첫 실행 시 `HandPicker`가 뜨고, **왼손 선택 시 포켓 표시·다이어그램·드릴 목표 핀이 모두 미러링**되는지 (D4의 7번 → 10번)
7. 왼손으로 D1~D4를 실제로 통과할 수 있는지 (미러링이 판정에만 적용되고 물리를 깨지 않는지)
8. `?lesson=B3` / `?area=D` 딥링크가 해당 지점을 바로 여는지
9. 레슨 완료 후 새로고침 → 진행률 복원, 초기화 버튼이 실제로 비우는지 (`HandPicker`도 다시 뜨는지)
10. **오답을 골랐을 때 그 오답에 맞는 피드백이 나오는지** (스트라이크 문항에서 "다음 1구만 더한 값"을 고르면 스페어 혼동 안내)
11. 드릴 실패 시에도 진행이 막히지 않는지, '도움 받기'가 실제로 쉬워지는지
12. **터치패드와 태블릿 터치로 드래그 투구가 되는지** (교실 환경)
13. 관찰 모드 슬로모션에서 물리 결과가 정상 속도와 질적으로 같은지 (스텝 횟수만 줄였으므로 같아야 한다)

**성능**: 오브젝트 12개 수준이라 여유롭다. 문제가 생기면 원인은 렌더가 아니라 1/120 물리 스텝이므로 그쪽부터 본다.

---

## 리스크와 대응

| 리스크 | 대응 |
|---|---|
| 핀이 넘어지는 모양이 어색함 (가장 흔한 문제) | 하부 콜라이더 density를 올려 무게중심을 낮춘다. 안 되면 콜라이더 4단으로 세분화 |
| 핀-핀 연쇄 충돌이 약함 | 핀 restitution ≈ 0.3, 마찰 낮게. 실제 핀은 잘 튄다 |
| **초등학생이 드래그 조작을 못 해 좌절** | 클램프 범위를 좁게 시작, 드릴에 '도움 받기'(조준 보조), 시도 횟수를 넉넉히, **실패해도 진행 가능**. 교육 목적은 통과가 아니라 이해다 |
| **본문이 초등 수준을 넘어 어려워짐** | 150자 제한을 유닛 테스트로 강제. 어려운 낱말은 용어집 팝업으로 빼낸다 |
| 왼손 미러링 누락으로 왼손 학생이 막힘 | `mirrorPin` 자기역함수 테스트 + 수동 검증 6·7번을 반드시 왼손으로 한 번 완주 |
| 자세 영역이 게임 조작 연습으로 오인됨 | C영역 상단 고정 배너 + 완료 조건을 드릴이 아닌 읽기/관찰로 제한 |
| 커리큘럼 18개의 오타·불일치 | 정합성 유닛 테스트로 구조적 오류를 기계 검출 (위 참조) |
| 볼링 규칙 서술의 정확성 | 수치는 `config.ts` 단일 출처를 인용. 지역 규정 차이가 있는 항목(공 무게 권장치 등)은 "보통"으로 서술 |
| `npm install` 네트워크 필요 | 첫 단계에서 설치 실패하면 즉시 알린다 |

## 설치할 패키지

```
three@^0.185          @dimforge/rapier3d-compat@^0.19   (런타임)
vite@^8  typescript  vitest                             (개발)
```

`rapier3d-compat`(비-compat이 아닌)를 쓴다 — top-level await 없이 `await RAPIER.init()`로 초기화되어 Vite 설정이 단순해진다. three 0.185는 자체 타입 선언을 포함하므로 `@types/three`는 불필요하다(TS가 타입을 못 찾는 경우에만 추가). UI는 DOM + Canvas2D로 직접 만들고 React 등 프레임워크는 넣지 않는다 — 패널 몇 개 수준에 빌드 복잡도를 추가할 이유가 없다.
