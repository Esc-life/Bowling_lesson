# 구현 현황

`PLAN.md`가 게임 본체(M0~M6)의 계획, 이 문서가 실제로 만들어진 것과 남은 것입니다.
1·2단계(플레이어 저장·자유 연습·같은 기기 다인 매치)의 설계는
`docs/superpowers/specs/2026-07-31-players-freeplay-match-design.md`, 3단계(온라인 실시간
대전)의 설계는 `docs/superpowers/specs/2026-08-01-online-match-design.md`에 있습니다.

검증 상태: **테스트 294개 통과 · 타입 검사 통과 · 프로덕션 빌드 성공 · 브라우저 실기 검증 완료**
(1·2단계는 실기에서 찾은 문제 6건 **전부 고쳤습니다** — 아래 "실기에서 찾은 문제" 참고.
3단계 온라인 실시간 대전을 새로 만들어 Supabase Realtime 프로젝트에 직접 연결해 두 개의 완전히
분리된 브라우저로 실기 검증까지 마쳤습니다 — 아래 "3단계 — 온라인 실시간 대전" 참고)

실행: `npm run dev` → http://localhost:5173/
딥링크: `?player=민준` `?lesson=B3` `?area=D` `?debug=1`

> `?hand=left`는 없어집니다. 투구 손은 이제 URL이 아니라 **플레이어의 속성**입니다.
> 공용 PC에서 링크 하나로 손이 바뀌면 다음 학생이 뒤집힌 레인에서 배우게 되기 때문입니다.
> `?player=` 없이 `?lesson=`/`?area=`만 주면 **시연 모드**로 열리고 진행률을 저장하지 않습니다.

> 온라인 대전을 쓰려면 `.env.local`에 `VITE_SUPABASE_URL`·`VITE_SUPABASE_ANON_KEY`가 있어야
> 합니다(git에는 없습니다 — `*.local` 규칙). 없으면 온라인 대전 버튼을 누르는 순간에만
> "설정할 수 없어요" 안내가 뜨고, 나머지 기능(배우기·자유 연습·같은 기기 대전)은 그대로 동작합니다.

---

## 완료 (M0 ~ M6, 게임 본체)

| 단계 | 내용 | 상태 |
|---|---|---|
| M0 | Vite + TypeScript + Three.js 스캐폴딩 | ✅ |
| M1 | Rapier 물리, 고정 1/120초 스텝, 복합 콜라이더 핀 10개 | ✅ |
| M2 | 드래그 스윙 입력, 조준선, 오일 구간 마찰 분리 | ✅ |
| M3 | `pinLayout`(좌우 미러링 포함), 정지 감지, `FrameMachine` | ✅ |
| M4 | `Scorecard` + 유닛 테스트 + DOM 점수판 | ✅ |
| M5 | 절차적 텍스처, 조명·그림자, 카메라 5개 모드 | ✅ |
| M6 | 관찰 모드 — 슬로모션, 궤적 리본, 오일 구간 표시, 핀 번호 | ✅ |

### 완료 (튜토리얼 로직 계층)

| 파일 | 내용 | 상태 |
|---|---|---|
| `tutorial/types.ts` | 데이터 모델 | ✅ |
| `tutorial/curriculum.ts` | **5개 영역 18개 레슨 전체 콘텐츠** | ✅ |
| `tutorial/distractors.ts` | 오답 자동 생성 + 오답별 피드백 | ✅ |
| `tutorial/TutorialFlow.ts` | 진행률·완료 판정 | ✅ |
| `tutorial/Progress.ts` | localStorage 저장·복원·초기화 | ✅ |
| `tutorial/QuizRunner.ts` | 4지선다 채점 | ✅ |

### 완료 (튜토리얼 화면)

| 파일 | 내용 | 상태 |
|---|---|---|
| `tutorial/glossary.ts` | 용어집 데이터 + 본문 자동 밑줄 링크 | ✅ |
| `tutorial/DrillRunner.ts` | 드릴 판정 (미러링·횟수·점수 목표) + 테스트 12개 | ✅ |
| `tutorial/diagrams/` | 핀 배치도 · 레인 상면도 · 점수판 · 4걸음 애니메이션 | ✅ |
| `ui/TutorialPanel.ts` | 레슨 본문 + 퀴즈 + 드릴/관찰 진입 | ✅ |
| `ui/AreaMenu.ts` | 영역 메뉴, 진행률, 이어서 하기, 초기화(2단계 확인) | ✅ |
| `ui/GlossaryPopover.ts` · `ui/ReportScreen.ts` | 용어 팝업 · 수료 리포트 | ✅ |
| `ui/TutorialUI.ts` | 전체 흐름 지휘 (메뉴↔레슨↔드릴/관찰, 저장) | ✅ |
| `main.ts` | '배우기' 버튼, 딥링크 `?lesson=` `?area=` | ✅ |

브라우저에서 실제로 확인한 것: A1(읽기 완료) → A2(퀴즈, 오답 피드백, 2/3 저장) →
D4 드릴(핀 7만 세워짐, 프로그램 투구로 성공 → 패널 복귀) → D3 관찰(데모 투구 2종,
궤적 리본, 3회 카운트 → 자동 완료) → C2(4걸음 애니메이션 재생 → 완료) →
B3 점수 문항(누적 빈칸 `?`, 오해 기반 오답 생성) → 리포트(28%, 영역별·퀴즈별).
왼손 플레이어에서 D4 드릴이 10번 핀으로 미러링되는 것도 확인.

### 완료 (플레이어 저장 · 자유 연습 · 다인 매치)

| 파일 | 내용 | 상태 |
|---|---|---|
| `players/PlayerStore.ts` · `players/types.ts` | **플레이어 저장** — 이름으로 구분, 진행률·투구 손이 사람별. 이름 검사(12자), 구버전 이관, 깨진 데이터 방어 | ✅ |
| `players/unlock.ts` | 18레슨 수료 판정, 잠금 사유 문구, 대전 참가 자격 | ✅ |
| `ui/PlayerPicker.ts` | 목록·만들기 화면, 손 고르기, 2단계 삭제 확인 | ✅ |
| `practice/PracticeSession.ts` · `ui/PracticeSetup.ts` | **자유 연습** — 10프레임 정식 경기 / 연습 투구(점수 없이 반복, 세울 핀 선택) | ✅ |
| `rules/MatchMachine.ts` · `ui/MatchSetup.ts` | **다인 매치** — 같은 기기 2~4명 번갈아, 프레임 수 3/5/10, 순위 | ✅ |
| `ui/Scoreboard.ts` | 다인 점수판(차례인 줄 강조), 순위 표시 | ✅ |
| `main.ts` | 홈(영역 메뉴)이 게임으로 가는 유일한 입구, 딥링크 `?player=`, 시연 모드 | ✅ |

플레이어·자유 연습·대전을 얹은 뒤 브라우저에서 다시 확인한 것:
빈 저장소 첫 실행(만들기 화면 직행) → 이름 12자 제한 → 손 버튼을 눌러도 입력한 이름 유지 →
민준(왼손)·서연(오른손) 생성, 서로의 진도가 섞이지 않음 → 삭제 2단계 확인에서 '그만둘래요' →
0/18에서 홈의 모든 버튼을 눌러도 레인에 못 들어감(홈에 ✕가 없고 레슨 패널의 ✕는 홈으로 돌아옴) →
18/18 해금 후 10프레임 경기 · 연습 투구(핀 1·3만 세워 "2개 중 1개", 5회 반복 동안 같은 배치 재설치) →
2인 3프레임 대전 완주(차례 배너, 점수판 줄 강조 이동, 왼손↔오른손 화살표 미러링,
마지막 프레임 보너스 투구 XXX, "민준 승리! 68점" + HUD 한 줄 일치) →
대전 중 홈 버튼이 `🚪 대전 그만두기`로 바뀌고 나가면 홈 위에 "대전을 그만뒀어요" 한 줄 →
딥링크 `?player=`(있는 이름/없는 이름), `?lesson=`만 주면 시연 모드 배지 + 저장 차단 확인 →
구버전 `progress.v1` 이관(화면에서 고른 손이 이김, 3/18 물려받고 옛 키 삭제) →
`quizScores`를 null로 망가뜨려도 앱이 정상 부팅.
전 구간 콘솔 예외 0건.

실기 42개 시나리오 중 **38개 통과 · 2개 실패 · 2개 주의**. 실패·주의 4건은
아래 "실기에서 찾은 문제"에 적었고, 그중 진도 소실 1건은 고쳤습니다.

## 3단계 — 온라인 실시간 대전

설계는 `docs/superpowers/specs/2026-08-01-online-match-design.md`. 서버 없는(GitHub Pages)
배포 위에 Supabase Realtime **Broadcast + Presence**만으로 동기화한다 — 테이블도 RLS도 없다.
"정답"을 어디에도 저장하지 않고, 각 기기가 "이번 굴림에 남은 핀" 로그를 순서대로 받아
`MatchMachine.settle()`을 그대로 재생해 스스로 계산한다. `FrameMachine`·`Scorecard`·
`MatchMachine`은 **한 줄도 건드리지 않았다** — 순수 로직이 이미 리플레이 가능하다는 것이
이 설계가 가능했던 이유다.

| 파일 | 내용 | 상태 |
|---|---|---|
| `net/supabaseClient.ts` | Supabase 클라이언트. `@supabase/supabase-js`를 동적 import로 받아 온라인 대전을 안 쓰는 학생의 초기 번들에 안 실리게 함 | ✅ |
| `net/roomCode.ts` | 방 코드 생성·형식 검사(4자, 헷갈리는 0/O·1/I 제외) | ✅ |
| `net/MatchSync.ts` | 롤 로그 → `MatchMachine` 재생 리듀서(순수). `apply()`의 applied/stale/gap/mismatch, `replay()`, `buildMatchFromLog()` | ✅ |
| `net/RoomChannel.ts` | Supabase Broadcast/Presence 어댑터. 방 코드 충돌 회피, 연결 끊김 감지 | ✅ |
| `net/OnlineMatchController.ts` | `Game`과 `RoomChannel`을 잇는 글루. `MatchSync`로 순번을 검증하고, 통과한 것만 `Game.applyRemoteThrow()`로 화면에 반영 | ✅ |
| `core/Game.ts` | `applyRemoteThrow()`(상대 결과를 굴러가는 연출 없이 즉시 반영), `onLocalRoll` 이벤트, `setAimGate()`(내 차례가 아니면 조준 막기) | ✅ |
| `ui/OnlineMatchSetup.ts` | 방 만들기·참가하기·로비(참가자 실시간 목록, 프레임 수 선택) | ✅ |
| `ui/AreaMenu.ts` · `main.ts` | "온라인 대전" 진입점 배선(기존 대전과 같은 수료 해금 규칙) | ✅ |

### 동작 원리 요약

같은 기기 다인 매치(2단계)와 마찬가지로 `MatchMachine` 하나를 여럿이 공유하는데, 이번엔
그 "여럿"이 서로 다른 기기다. 내 차례에 실제로 물리를 굴려 정지하면(`Game.updateThrowProgress`)
`onLocalRoll`이 "이번 굴림에 남은 핀" 배열을 들고 발화하고, `OnlineMatchController`가 순번을
붙여 Supabase Broadcast로 방송한다. 상대는 그 배열을 받아 `MatchMachine.settle()`에 그대로
넣는다 — 물리를 다시 계산하지 않고, 이미 검증된 순수 함수가 나와 상대에게서 **완전히 같은
점수**를 낸다. 상대가 던지는 동안 내 화면은 조준이 막힌 "간단한 대기 화면"이다(3D 리플레이는
이번 스코프에서 뺐다 — 스펙 문서 참고).

순번(`seq`)이 어긋나면(Broadcast는 순서 보장이 약하다) `requestState`/`stateSnapshot`으로
로그를 처음부터 다시 재생해 맞춘다. 이 gap/stale/mismatch 판정과 재생 로직은
`net/MatchSync.test.ts`·`net/OnlineMatchController.test.ts`(가짜 `Game`/`RoomChannel`로
로컬·원격 굴림, 중복, 구멍, 스냅샷 반영을 전부 검증) 28개 테스트로 촘촘히 덮여 있다.

### 실기 검증

실제 Supabase 프로젝트(사용자가 제공한 URL·anon key, `.env.local`)에 연결해 확인했다.

- Node 스크립트로 Broadcast/Presence 왕복을 먼저 확인 — 클라이언트 두 개(각자 별도
  websocket)로 방을 만들고 참가한 뒤 메시지가 정상 전달되는지 (Supabase 자체는 문제없음을
  먼저 격리해서 확인).
- Playwright로 **완전히 분리된 브라우저 프로세스 두 개**(탭 두 개가 아니라 — 이유는 아래
  "실기에서 찾은 문제" 참고)를 각각 다른 플레이어(민준/서연)로 띄워:
  - 방 만들기(민준) → 코드 표시 → 참가하기(서연, 같은 코드) → 양쪽 로비에 참가자 2명 실시간
    반영 확인
  - 방장이 3프레임으로 시작 → 양쪽 다 로비를 벗어나 경기 화면으로 전환
  - **실제 물리로** 세 번 번갈아 던지고(민준→서연→민준), **매 투구 뒤마다** 두 기기의
    점수판(`MatchMachine.ranking`)이 완전히 같은지 확인 — 전부 일치
  - 존재하지 않는 방 코드(`ZZZZ`)로 참가 시도 → "방을 찾을 수 없어요" 안내가 뜨는지 확인
- 전 구간 콘솔 예외 0건(연결·방송·반영 경로에서).

**전체 매치를 끝까지는 돌리지 못했다.** 이 headless 샌드박스는 GPU 가속이 없어 물리
시뮬레이션이 실제 시간보다 훨씬 느리게 돈다(실측: 물리 5초를 계산하는 데 실제로는 30초 이상
걸림 — 데스크톱 실기에서는 STATUS.md의 기존 실측대로 투구당 3~7초여야 정상이다). 3프레임
경기를 끝까지 돌리면 최소 수 분이 걸려, 검증 범위를 "물리 → 방송 → 상대 화면 반영" 경로가
실제로 동작하는지 확인하는 데 필요한 최소한(투구 3번, 양방향 모두 최소 한 번씩)으로 좁혔다.
점수 계산 규칙 자체(스트라이크·스페어·마지막 프레임 보너스 등)는 `MatchMachine`·`FrameMachine`
유닛 테스트가 이미 훨씬 촘촘히 검증하고 있고, 이번 3단계가 건드리지 않은 부분이라 다시
실기로 볼 필요는 적다고 판단했다. **일반 데스크톱 브라우저(교실 PC)에서는 이 속도 문제가
없을 것으로 예상하지만, 실제로 데스크톱에서 전체 매치를 완주해 보지는 못했다** — 확인 못한
것으로 정직하게 남겨 둔다.

### 실기에서 찾은 문제 (전부 고쳤음)

1. **Supabase Realtime은 `subscribe()` 뒤에 `presence` 리스너를 추가하면 던진다.**
   ("cannot add presence callbacks ... after subscribe()") `RoomChannel`이 인스턴스를 만들고
   나서(=구독이 끝나고 나서) 리스너를 붙이는 구조였는데, 인스턴스가 구독보다 늦게 만들어져야
   하는 상황(방 코드 충돌 확인, 참가자 존재 확인)과 맞지 않았다. → 리스너를 담는 `EventsBox`를
   구독 **전에** 채널에 등록해 두고, 나중에 만들어지는 `RoomChannel` 인스턴스가 그 상자를
   그대로 물려받게 바꿨다.
2. **브라우저 하나에 탭 두 개로 두면 백그라운드 탭의 타이머/물리가 멈춘 것처럼 보인다.**
   Chromium이 배경 탭의 `requestAnimationFrame`·`setTimeout`을 강하게 스로틀링해서, 상대가
   던지는 동안 내 탭이 배경이면 `input.enabled`가 영영 안 열리는 것처럼 보였다(앱 버그
   아님 — 이 프로젝트의 실기 검증 도구 쪽 문제). → 두 "기기"를 완전히 분리된 브라우저
   프로세스로 띄우도록 검증 스크립트를 고쳤다. 실제 사용자는 애초에 서로 다른 기기이므로
   이 문제를 겪지 않는다.
3. **`machine.phase`가 `aiming`으로 돌아온 것과 `input.enabled`가 열린 것은 다른 시점이다.**
   `settle()`은 phase를 곧장 되돌리지만, 실제로 핀을 다시 세우고 입력을 여는
   `beginAiming()`은 결과를 보여주는 1400ms 뒤에야 실행된다(연속 시연 투구 경합 상태를
   고친 것과 같은 지점 — 위 "조사하다 만 것" 참고). 검증 스크립트가 이 둘을 섞어서 보다가
   교착 상태에 빠졌었다 — 앱 코드가 아니라 검증 스크립트의 오해였다.

### 알려진 한계 (일부러 뺀 것 · 아직 확인 못 한 것)

- **3D 리플레이 없음.** 상대가 던지는 동안 내 화면은 대기 화면이다(스펙에서 이미 이렇게
  정함 — "간단한 대기 화면" 선택).
- **서버에 정답을 저장하지 않는다.** 방에 아무도 안 남으면 그 매치의 로그도 함께 사라진다.
  재접속은 방에 남아 있는 다른 사람이 있을 때만 `requestState`로 복구된다. 이 경로는
  `net/OnlineMatchController.test.ts`로 로직만 검증했고, **실제 두 브라우저로 접속 → 끊김 →
  재접속 → 복구까지는 실기로 확인하지 못했다.**
- **방 코드 공간이 좁다.** 4자(헷갈리는 문자 제외 기준 약 100만 개)라 학교 하나에서 동시에
  여러 방이 열려도 충돌 확률은 낮지만 0은 아니다. 방을 만들 때 Presence로 충돌을 확인하고
  다시 뽑는 안전장치는 있다.
- **온라인 대전 중 홈으로 나가도 상대에게 알리지 않는다.** 같은 기기 대전처럼 "그만뒀어요"
  안내가 상대 화면에 뜨지 않는다 — 상대는 내가 다시 던질 때까지 대기 화면에 머문다. 다음에
  손볼 수 있는 지점으로 남겨 둔다.
- **데스크톱 브라우저에서 전체 매치 완주를 실기로 못 봤다** (위 "실기 검증" 절 참고).

### 드릴이 게임 규칙과 공존하는 방법 (설계 메모)

핀 일부만 세우는 드릴에서도 `FrameMachine`은 그대로 돈다. 점수는 쓰지 않으므로
**매 시도 전에 `machine.reset()` + `setupDrill()`** 로 되돌린다(시도 12번이 쌓여도
"게임 종료"에 걸리지 않는다). 이때 기계가 계산한 "스트라이크!" 문구는 틀리므로
드릴 중에는 기본 배너를 끄고 드릴 바가 자체 문구를 낸다. 점수 드릴(E2)만
진짜 게임 그대로 진행하고 점수판도 그대로 보여준다.

---

## 계획에서 바뀐 것 (구현하며 찾은 오류)

계획대로 되지 않아 **고친 것들**입니다. 계획서의 해당 부분은 이제 틀립니다.

### 1. 훅(공이 휘는 것)의 물리 모델이 틀렸다 — 가장 중요

계획은 "회전은 릴리즈 시 **Y축** 각속도"라고 썼습니다. **이러면 공은 절대 휘지 않습니다.**

접지점의 미끄럼 속도는 `v + ω×r`이고 `r = (0, -R, 0)`입니다. `ω`가 Y축과 나란하면
`ω×r = 0`이 되어 마찰에 좌우 성분이 아예 생기지 않습니다. 실측 결과 18m를 굴러가며
**7mm** 휘었습니다(= 직구).

→ 회전을 **진행 방향(Z축)** 으로 주도록 고쳤습니다. `ω×r = (ωz·R, 0, 0)`이 되어 좌우
미끄럼이 생기고, 마찰이 그것을 막으면서 공을 옆으로 밀어냅니다. 실제 볼링에서
회전축을 진행 방향으로 기울여 던지는 것과 같은 원리입니다.

### 2. 오일/드라이 마찰값이 훅을 반대 모양으로 만들었다

계획값(오일 0.04 / 드라이 0.20)에서는 휘어짐의 **71%가 오일 구간**에서 일어났습니다.
레슨 D3이 "뒤쪽 마른 곳에서 휜다"고 가르치는데 화면은 반대로 보이는 상태였습니다.

→ 실측으로 **오일 0.008 / 드라이 0.50** 으로 고쳤습니다. 앞에서 9cm 미끄러지다
뒤에서 43cm 확 휘는(드라이 82%) 실제 볼링의 모양이 나옵니다.

### 3. 좌우 부호 규칙이 화면과 뒤집혀 있었다

계획의 핀 배치는 "3번 핀(투구자의 오른쪽)이 +x"였습니다. 그런데 투구자는 +Z를 보고
서 있고, 오른손 좌표계에서 **+Z를 보는 사람의 오른손은 -X** 를 가리킵니다. 그대로 두면
오른손잡이 학생이 **화면 왼쪽**에 있는 화살표를 "오른쪽에서 두 번째"라고 배웁니다.

→ `투구자의 오른쪽 = 월드 -X = 화면 오른쪽`으로 통일했습니다. HUD의 ◀▶ 버튼도
같은 이유로 부호를 뒤집었습니다. 이 규칙을 못박는 테스트를 넣어 두었습니다.

### 4. 핀 무게중심 — 계획의 처방이 필요하지 않았다

계획은 "하부 콜라이더 density를 높여 무게중심을 낮춘다"였습니다. 실제로 계산해 보니
**아래가 두꺼운 테이퍼 모양 자체가** 무게중심을 핀 높이의 35.7%에 놓습니다(실제 볼링
핀과 거의 같음). density를 세게 실으면 오히려 27%까지 내려가 핀이 잘 안 넘어집니다.

→ density는 살짝만(1.15 / 1.0 / 0.9) 실어 34%를 맞췄습니다.

### 5. 얇은 바닥은 핀이 통과한다

레인을 보이는 두께(8cm)대로 물리 콜라이더로 만들었더니, 세게 맞은 핀이 한 스텝에
판을 통과해 **y = -34m** 까지 떨어졌습니다. 핏(공이 떨어지는 곳)도 옆이 뚫려 있어
공이 밖으로 빠져나갔습니다.

→ 물리 바닥을 두꺼운 덩어리(반두께 0.5m)로 바꾸고, 측면 벽을 핏 구간까지 연장하고,
핀에 CCD를 켰습니다. 맨 아래에 넓은 안전망 바닥도 깔았습니다.

### 6. 정지 판정이 매 투구를 7초로 늘렸다

계획은 "모든 강체 속도가 임계값 이하로 0.5초 연속"이었습니다. 그런데 공과 넘어진 핀이
핏 바닥에서 한참 굴러다녀서, 실제로는 **거의 매번 하드 타임아웃(7초)** 까지 갔습니다.

→ **결과를 바꿀 수 있는 것만** 봅니다. 핏에 떨어진 공과 핀은 이미 확정되었고 다른 핀을
건드릴 수도 없으므로 제외합니다. 정지까지 2.8~5.7초로 줄었습니다.

### 7. `@types/three`가 필요했다

계획은 "three 0.185는 자체 타입 선언을 포함하므로 불필요"라고 썼습니다. 실제로는
`tsc`가 타입을 찾지 못해 `@types/three@0.185.1`을 설치했습니다.

### 8. 조준과 스핀이 뒤엉켜 있었다 (사용자 피드백으로 발견)

"좌우 방향에 의해 스핀이 생기는 게 정확하지 않다"는 피드백의 원인은 두 가지였습니다.

- **조준만 해도 스핀이 걸렸다.** 스핀을 놓기 직전 120ms의 **절대** 좌우 속도로
  계산했는데, 각도를 주려고 비스듬히 당기는 동안에도 좌우 속도는 계속 생긴다.
  → 당김 기울기(조준선)만큼의 좌우 속도를 빼고, 거기서 **벗어난 만큼**(놓기 직전의
  '꺾기')만 스핀으로 센다. 직선으로 비스듬히 당기면 스핀 0.
- **조준선 미리보기가 반대로 휘었다.** Z축 스핀 +는 마찰이 공을 월드 -X(화면
  오른쪽)로 밀어내는데, `AimGuide`는 곡선을 +X로 그렸다. → 부호 반전.
  실측: 스핀 +12 데모 투구가 x +0.15에서 출발해 헤드핀을 -0.29로 통과(화면
  오른쪽으로 44cm 훅) — 오른쪽으로 꺾으면 오른쪽으로 휘는 직관과 일치.

### 9. `hidden` 속성은 클래스 display에 진다

`.overlay { display: grid }`가 `hidden` 속성(UA의 `display:none`)을 이겨서,
숨겨 둔 오버레이 3장이 화면 전체를 덮었습니다. → 전역 `[hidden] { display:
none !important }` 규칙으로 못박음.

### 10. 드릴 힌트에 왼쪽/오른쪽을 쓰면 안 된다

D4 힌트가 "왼쪽 끝 핀"이었는데, 왼손 학생에게는 핀이 10번(화면 오른쪽)으로
미러링되므로 틀린 방향을 알려주게 됩니다. → 방향 중립 문구로 교체.
핀 배치는 미러링돼도 **글은 미러링되지 않는다** — 콘텐츠 작성 규칙에 추가.

### 11. 그 밖의 작은 버그

- `renderer.setSize(w, h, false)` — 세 번째 인자가 캔버스 CSS 크기 갱신을 끄기 때문에
  캔버스가 devicePixelRatio 배로 커져 좌상단 1/4만 보였습니다.
- 손으로 만든 화살표 삼각형의 정점 순서가 시계 방향이어서 법선이 아래를 향했고,
  기본 앞면 컬링에 걸려 **화살표 7개가 전부 안 보였습니다.**
- 조준 카메라가 공에 너무 가까워(2.3m) 공이 화면 아래로 잘렸습니다 → 4.3m로 물림.
- 오일 구간 표시가 조명에 묻혀 안 보였습니다(투명도 0.16 → 0.38). 교구는 명확해야 합니다.
- 상면도 카메라가 수직이라 12m 폭이 잡히고 1m 레인이 실처럼 보였습니다 → 비스듬히.
- 점수판이 하단 조작 UI와 겹쳤습니다 → 위로 옮김(실제 볼링장도 위에 있습니다).

---

## 실측으로 확인한 것

브라우저에서 물리를 직접 돌려 숫자로 확인했습니다.

- **공 속도** 최대 파워 8.9 m/s, 18.29m를 약 2.1초에 주행 (실제 볼링과 같은 수준)
- **회전 0이면 완전한 직구**: 헤드핀 통과 x가 정확히 0.000
- **포켓 조준(x=+0.076) → 스트라이크 10개**, **정중앙 조준 → 4개만** 넘어짐
  (실제 볼링에서 정면 충돌이 스플릿을 남기는 것과 일치 — 레슨 D2의 근거)
- **좌우 대칭**: 오른손 포켓(+0.088)과 왼손 포켓(-0.088) 모두 스트라이크
- **훅 크기**: 8m/s에서 회전 4→17cm, 12→37cm, 20→53cm, 28→66cm (포화 없이 선형에 가까움)
- **느린 공이 더 휜다**: 6m/s에서 회전 8이 40cm (8m/s에서는 28cm) — 오일 구간에 오래
  있기 때문. 계획에 없었지만 실제 볼링과 같고 좋은 교육 소재입니다.
- **10프레임 완주 통합 테스트**: 물리로 굴린 결과를 규칙 계층에 넣어 점수를 손계산과
  대조 → `5,2|7,0|5,2|7,3|7,0|7,2|6,3|9,0|7,0|4,2` → 85점, 누적 전부 일치.
  스페어 상황에서 넘어진 핀만 치우고 서 있는 핀을 유지하는 것도 확인.

### 대전에 걸리는 시간 (수업 설계용)

`?debug=1`의 숫자 키로 **4명 3프레임 매치를 완주**시켜 투구 수를 셌습니다.

- **투구 수 25회** (스트라이크·스페어 분포에 따라 **20~28회**.
  전원 매 프레임 스트라이크면 20회, 전원 오픈 프레임 + 마지막 프레임 스페어면 28회)
- **투구 1회당 정지까지** 실측 3.2 / 3.6 / 4.3 / 5.9 / 7.0초 — **평균 약 4.8초**
  (7.0초는 정지 판정 하드 타임아웃에 걸린 경우)
- → **공이 움직이는 시간만 약 2분.** 여기에 학생이 자리를 잡고 조준해 드래그하는 시간
  (한 번에 5~10초로 잡으면)을 더하면 **4명 3프레임은 대략 4~6분**입니다.
- 같은 계산으로 **4명 10프레임은 80~90투구 → 12~18분**이라, 한 차시에는 3프레임이나
  5프레임을 권합니다.

### 확인하지 못한 것

- ~~실제 마우스 드래그 조작감.~~ **→ 좀 더 확인했습니다.** 예전엔 단일 합성
  PointerEvent 한 번으로만 찍어 봤는데, 이번엔 Playwright `page.mouse`로 12단계
  중간 지점 + 매 20ms 간격을 넣어 실제 드래그에 가깝게 재현했습니다(아래로 당기며
  오른쪽으로 살짝 꺾기). 파워 8.99m/s(최대 근접), 훅 +0.2rad/s로 정상 산출.
  **다만 실제 아이 손의 드래그 "느낌"은 여전히 사람이 직접 해 봐야 압니다** — 이건
  자동화로 완전히 대체할 수 없는 부분입니다.
- ~~터치패드·태블릿 실기기 확인.~~ **→ 자동화로 일부 닫았습니다.** CDP
  `Input.dispatchTouchEvent`로 실제 터치 이벤트(touchStart/Move/End)를 만들어
  같은 스윙을 재현 — 마우스와 동일하게 파워 8.99m/s, 훅 -0.2rad/s(반대쪽으로
  꺾어서 반대 훅)로 정상 동작했습니다. `Input.ts`가 Pointer Events 하나로
  마우스·터치를 함께 받는다는 설계 그대로입니다. **진짜 태블릿 화면 크기·손가락
  두께·핀치 줌 같은 것은 여전히 실기기가 필요합니다.**
- **온라인 실시간 대전(3단계)은 아직 손대지 않았습니다.** 지금의 다인 매치는 전부
  **같은 기기에서 번갈아 치는 방식**입니다. 서버도, 방 코드도, 네트워크 동기화도 없습니다.
- ~~진짜 모바일 폭(390px)에서의 확인.~~ **→ 확인하고 문제 하나를 고쳤습니다.**
  Playwright 뷰포트를 390×844로 실제로 줄여 봤습니다(수동 브라우저 창은 501px
  밑으로 못 줄였지만 자동화 뷰포트는 임의 크기가 됩니다). 홈·대전 만들기 화면은
  가로 스크롤 없이 깨끗했습니다. **솔로 점수판에서 문제를 하나 찾았습니다**:
  프레임 10칸 + 총점이 390px에 다 안 들어가 점수판이 옆으로 스크롤되는데,
  총점(`.total`)이 스크롤해야만 보이는 위치에 있어서 경기 중 가장 자주 보는
  숫자(지금 몇 점인지)가 안 보였습니다. `.total`에 `position: sticky; right: 0`을
  줘서 어느 프레임을 스크롤해 보고 있어도 총점은 항상 오른쪽에 붙박여 보이도록
  고쳤습니다(`styles.css`, 640px 이하 미디어쿼리). 고치기 전/후 모두 스크린샷과
  좌표(`scrollWidth`/`clientWidth`/`getBoundingClientRect`)로 확인했습니다.
- ~~배포 번들이 2.8MB(gzip 989KB)입니다.~~ **→ 코드 분할했습니다.** `main.ts`의
  `import { Game } from './core/Game'`를 동적 `import()`로 바꿔, Three.js·Rapier가
  들어 있는 `Game.ts`를 별도 청크로 뺐습니다. 첫 화면(플레이어 고르기 등)이 받는
  청크는 `index-*.js` 94.83KB(gzip 29.55KB)로 줄었고, 나머지 2.79MB(gzip 982KB)는
  `Game-*.js`로 분리돼 실제로 게임이 만들어지는 시점에 따로 받습니다. `Game` 값은
  main.ts에서 딱 한 곳(`Game.create(stage)`)에만 쓰여서 위험이 적었습니다.
  Playwright로 홈 화면 진입 → 자유 연습 → 10프레임 경기 → 디버그 판정까지
  다시 재생시켜 정상 동작을 확인했습니다.

### 실기에서 찾은 문제

브라우저에서 직접 확인한 것만 적습니다. **1·2·3·4·6번은 고쳤고, 5번은 절반만(색) 고쳤습니다.**
고친 것은 전부 Playwright(`.claude/skills/playwright-skill`, 헤드리스)로 다시 눌러 보고 확인했습니다.

1. ~~**경기 도중 🏠을 누르면 점수를 되찾을 길이 없다.**~~ **→ 고쳤습니다.** 10프레임
   경기를 5프레임까지 친 뒤 🏠을 누르면 매치 객체 자체는 5프레임·32점 그대로
   남습니다(설계 의도대로 `openHome()`이 혼자 치는 경기는 되돌리지 않습니다). 그런데
   **홈에서 그 경기로 돌아가는 버튼이 없었습니다.** 다시 들어가는 유일한 길인
   `자유 연습 → 10프레임 경기`가 `startFreeGame()` → `resetSolo()`로 새 매치를 만들어
   버려서, 학생 눈에는 점수가 그냥 사라졌습니다.
   → 홈에 "🎳 치던 경기로 돌아가기 (N프레임째 · M점)" 버튼을 추가했습니다.
   `TutorialUI.openMenu()`가 그릴 때마다 `hooks.getResumableGame()`을 다시 물어보게
   해서, 레슨 패널 ✕처럼 `main.ts`의 `openHome()`을 거치지 않는 내부 경로에서도
   버튼이 낡은 값으로 사라지지 않습니다. Playwright로 2프레임째·8점 상태에서
   🏠 → "돌아가기" 클릭 → 같은 프레임·점수로 복귀하는 것까지 확인했습니다.
   **이어서 찾았던 남은 구멍도 마저 고쳤습니다**: 이 상태에서 레슨을 열어 드릴을
   시작하면 `beginOwnMatch()`가 자기 매치로 갈아 끼우면서 치던 경기가 조용히
   사라지던 문제. `TutorialUI`에 `savedMatch` 필드를 두어, 드릴/관찰을 시작하기
   직전에 `saveMatchIfResumable()`로 "혼자 치는 + 한 번은 던졌음 + 안 끝남" 조건을
   만족하는 매치만 저장해 두고, 세션이 끝나면(`quitSession`/`abandonSession`)
   `restoreOrBeginOwnMatch()`가 저장해 둔 매치가 있으면 되돌리고 없으면 늘 하던 대로
   새로 만듭니다. Playwright로 1프레임째 던진 뒤 🏠 → D4 레슨 → 드릴 시작 → 그만하기
   → 레슨 ✕ → 🏠까지 왕복해 "돌아가기" 버튼이 여전히 같은 프레임·점수를 가리키는 것을
   확인했습니다.
2. ~~**다인 점수판이 좁은 화면에서 자기 줄을 못 보여 준다.**~~ **→ 고쳤습니다.** 4명
   대전을 501px 폭에서 열면 점수판이 가로 한 줄(flex-row)로 `scrollWidth 778px` >
   `clientWidth 481px`이 되어 3번째 학생부터 잘리고 4번째는 아예 화면 밖(x=593)이었
   습니다. `overflow-x: auto`라 손으로 밀면 보이긴 했지만, 차례가 온 사람의 줄로
   자동 스크롤하지도 않았습니다.
   → `.scoreboard--match`가 부모(`.scoreboard`)의 기본값(`flex-row`)을 그대로
   물려받던 게 원인이라 `flex-direction: column`으로 세로 고정했습니다. 사람 수와
   무관하게 항상 전원이 보입니다. 차례가 바뀐 순간에만(매 프레임 다시 그리므로,
   매번 스크롤하면 사용자 스크롤과 싸우게 됩니다) `match-row--active`를
   `scrollIntoView`. Playwright로 501px 폭 재현: 4명 전원이 x=16~485
   (clientWidth 501) 안에, 서로 다른 y로 세로로 쌓인 것을 확인했습니다.
3. ~~**좁은 폭에서 대전 종료 버튼이 🚪 아이콘 하나로 줄어든다.**~~ **→ 고쳤습니다.**
   `.btn-label`이 `display: none`이 되어 "대전 그만두기" 글자가 사라졌습니다.
   기능은 정상이고 `aria-label`도 붙어 있었지만, 초등학생에게 문 이모지 하나로
   "판을 끝낸다"는 뜻이 읽히지 않았습니다.
   → `.learn-btn.is-quit .btn-label`만 좁은 화면에서도 다시 보이게 예외를 뒀습니다.
   버튼이 화면 오른쪽 끝에 고정돼 있어 글자가 늘어도 왼쪽으로만 넓어지므로
   restart 버튼과 안 겹칩니다. Playwright로 501px 폭에서 "🚪 대전 그만두기" 글자가
   그대로 보이는 것을 확인했습니다.
4. ~~**`quizScores`가 망가지면 배운 레슨까지 함께 지워진다.**~~ **→ 고쳤습니다.**
   `sanitizePlayer()`가 `isProgressState()`를 통과하지 못한 progress를 통째로
   `emptyProgress()`로 바꾸는 바람에, 퀴즈 점수 하나가 깨졌을 뿐인데 3/18이 0/18이
   됐습니다(실측). 부팅 크래시를 막으려고 넣은 검사가 조용한 진도 소실을 만든
   셈이었습니다. **진행률을 필드별로 되살리도록** 고쳤습니다 — `quizScores`가 깨져도
   배운 레슨은 남고, `completedLessons`가 깨져도 퀴즈 점수는 남습니다. 구버전 이관도
   같은 경로를 타므로 옛 학생이 이관하는 순간 배운 것을 잃지 않습니다. 테스트 3개 추가.
5. ~~**연습 투구의 핀 고르기 화면이 읽기 어렵다.**~~ **→ 고쳤습니다.** 켠 핀은
   `#f2f2f2`, 끈 핀은 크림색 패널 위 `rgba(255,255,255,0.08)`이라 거의 같은 색으로
   보이던 것을, 켠 핀은 초록(`--felt-green`)으로 확 켜지고 끈 핀은 흐린 회색으로
   가라앉게 고쳤습니다(Playwright 스크린샷으로 확인).
   **남아 있던 절반(모양)도 마저 고쳤습니다.** 1~10이 4칸씩 끊긴 격자라 A2에서 배운
   볼링 삼각형과 모양이 달랐던 것을, `PracticeSetup.ts`의 `rackHtml()`을 `pinLayout.ts`의
   `pinRow()`(0=헤드핀~3=뒷줄)로 4행(1·2·3·4개씩)으로 다시 묶고, `.pin-triangle` +
   `.pin-row`(각 행 flex justify-content: center)로 CSS를 바꿔 열 계산 없이 저절로
   삼각형이 되게 했습니다. Playwright로 행 구성이 `[7,8,9,10] [4,5,6] [2,3] [1]`인
   것과 스크린샷을 확인했습니다.
6. ~~**만들기 화면에서 고른 손이 눈에 잘 안 띈다.**~~ **→ 고쳤습니다.** 선택 표시가
   크림색 2px 외곽선 하나뿐이라 파란 버튼 두 개가 거의 같아 보였습니다.
   → 안 고른 손은 흐린 회색으로 가라앉히고, 고른 손만 파랗게 켜지도록 바꿨습니다.
   Playwright 스크린샷으로 확인.

### ~~조사하다 만 것~~ → 원인 찾아 고쳤습니다

프로그램으로 투구를 연달아(앞 공이 멈춘 뒤 0.3초 만에) 던지면 공이 **핀을 그대로
통과해** 0개로 기록되는 장면을 여러 번 봤던 문제. 스로틀링 탓인지 진짜 버그인지
가르지 못하고 남겨 뒀었는데, **환경과 무관한 진짜 경합 상태(race condition)**
였습니다.

**원인**: 한 투구가 멈추면 `Game.updateThrowProgress()`가 `FrameMachine.settle()`을
불러 `machine.phase`를 그 자리에서 곧장 `'aiming'`으로 되돌립니다(`FrameMachine.ts:165`).
그런데 핀을 실제로 다시 세우고 공을 제자리에 두는 `beginAiming()`은 결과 문구를
보여주려고 **1400ms 뒤에야** 실행됩니다(`Game.ts`의 `updateThrowProgress` 안
`window.setTimeout(..., 1400)`). 즉 `phase`만 보면 그 1400ms 동안도 이미
"조준 가능"으로 보입니다. 사람의 드래그는 `Input.enabled`가 이 창을 막아 주지만
(`beginAiming()`이 실행돼야 `true`가 됨), 레슨 D3의 시연 투구가 쓰는
`Game.throwProgrammatically()` → `throwBall()`은 `machine.phase`만 확인하고
`input.enabled`를 보지 않아서, 이 1400ms 창 안에 두 번째 시연 투구를 누르면
**이전 핀이 그대로 남아 있는 레인으로 새 공이 던져졌습니다.**

**고친 곳**: `Game.throwBall()`의 진입 조건에 `!this.input.enabled`를 추가
(`src/core/Game.ts`). 이제 `beginAiming()`이 실제로 핀·공을 되돌려 놓기 전까지는
프로그램 투구도 막힙니다.

**검증**: Playwright로 D3 레슨의 "곧은 공 굴리기" → 정지 직후(핀 `4,6,7,10`
남음) → 즉시 "휘는 공 굴리기" 재클릭 → **고치기 전에는** 400ms 만에 서 있는
핀이 이유 없이 10개로 튀어 버렸고(진짜 재현 성공), **고친 뒤에는** 1400ms 창이
끝날 때까지 핀 `4,6,7,10`이 그대로 유지되고 두 번째 클릭은 조용히 무시됨,
창이 끝난 뒤 다시 누르면 정상적으로 던져지는 것을 확인했습니다.

---

## 다음에 할 일 (인계)

이 작업은 `main`에 반영되어 있습니다(과거 작업 브랜치였던 `feat/players-freeplay-match`는
지금 `main`과 동일한 커밋을 가리킵니다).

### 바로 이어서 할 것 — 새로 찾은 것 · 남겨 둔 것

1·2단계는 이전 우선순위 5건 + 마지막 2건(드릴이 진행 중이던 솔로 경기를 잡아먹는 문제,
핀 고르기가 격자였던 것)까지 전부 고쳐 Playwright 실기 검증을 마쳤습니다
(위 "실기에서 찾은 문제" 1·5번 참고).

3단계(온라인 실시간 대전)는 이번에 새로 만들어 `main`에 반영했습니다 — 자세한 내용은
위 "3단계 — 온라인 실시간 대전" 절 참고. **남은 것 — 위 "알려진 한계"에 이미 적었지만
인계 항목으로 다시 정리하면:**

- **데스크톱 브라우저에서 전체 매치(3~10프레임) 완주를 실기로 확인하지 못했습니다.**
  headless 샌드박스의 물리 속도 문제 때문에 검증 범위를 투구 3번(양방향 각 한 번 이상)으로
  좁혔습니다. 실제 교실 PC에서 한 번은 3프레임 온라인 대전을 끝까지 돌려 보는 것을 권합니다.
- **재접속(끊김 → 같은 코드로 재입장 → `requestState` 복구)을 실기로 확인하지 못했습니다.**
  로직은 `net/OnlineMatchController.test.ts`로 검증했지만, 실제 네트워크 끊김·Supabase
  재연결 타이밍까지 재현하지는 않았습니다.
- **온라인 대전 중 홈으로 나가도 상대에게 알림이 가지 않습니다.** 같은 기기 대전의
  "그만뒀어요" 안내에 대응하는 것이 온라인에는 없습니다.
- Supabase 프로젝트에 테이블을 하나도 안 만들었으므로 **온라인 대전 데이터가 어디에도
  영속 저장되지 않습니다** — 설계상 의도(스펙 참고)이지만, 나중에 "지난 대전 기록"류
  기능을 원하면 이 부분부터 다시 설계해야 합니다.

### 브라우저 실기 검증 도구

`.claude/skills/playwright-skill`에 Playwright 기반 브라우저 자동화 스킬을
프로젝트 전용으로 설치해 뒀습니다(https://github.com/lackeyjb/playwright-skill,
MIT). `node_modules`와 실행 중 남는 `.temp-execution-*.js`는 `.gitignore`에서
제외됩니다. 이 디스플레이 없는 환경에서는 `headless: true`로 돌렸고, Chromium
구동에 필요한 시스템 라이브러리는 `sudo npx playwright install-deps chromium`으로
한 번 설치했습니다(Xvfb도 같이 깔려서 headed 모드도 가능합니다).

18레슨을 다 깨야 열리는 자유 연습·대전을 테스트할 때는 실제 UI로 클리어하는 대신
`localStorage`의 `bowling3d.players.v1`에 `completedLessons`를 채운 플레이어를
직접 시드했습니다 — 정상적인 e2e 지름길이지만, 이 방식이 실제 진행률 저장 형태
(`players/types.ts`의 `Player`, `tutorial/TutorialFlow.ts`의 `ProgressState`)와
어긋나면 조용히 실패하므로 스키마가 바뀌면 시드 스크립트도 같이 손봐야 합니다.

### 3단계 진행 상황

처음부터 3단계로 나눠 설계했고, **셋 다 `main`에 반영돼 있습니다.**

- 1단계 플레이어 이름 저장 ✅
- 2단계 자유 연습 + 같은 기기 다인 매치 ✅
- 3단계 온라인 실시간 대전 ✅ (Supabase Realtime Broadcast+Presence, 서버 테이블 없음 —
  자세한 내용은 위 "3단계 — 온라인 실시간 대전" 절과
  `docs/superpowers/specs/2026-08-01-online-match-design.md` 참고)

### 참고 문서

- `docs/superpowers/specs/2026-07-31-players-freeplay-match-design.md` — 1·2단계 설계와 결정 근거
- `docs/superpowers/plans/2026-07-31-players-freeplay-match.md` — 1·2단계 구현 계획
- `docs/superpowers/specs/2026-08-01-online-match-design.md` — 3단계(온라인 실시간 대전) 설계와 결정 근거

**계획서는 일부 틀립니다.** 구현·리뷰 과정에서 계획서 결함 10건이 발견돼 고쳤습니다.
계획대로 하면 안 되는 지점 중 특히 중요한 넷:

- 턴 넘김에 `ThrowResult.frameCompleted`를 쓰면 **마지막 프레임 보너스 투구를 다음
  사람이 던지게 됩니다.** `currentFrame` 변화와 `isGameOver`로 판정해야 합니다.
- `soloMatch()`에 손을 넘기지 않으면 `restart()`가 왼손 학생의 레인을 오른손으로
  되돌립니다.
- 연습 투구에서 `result.knockedCount`를 그대로 쓰면 `PracticeSession.record`가
  예외를 던져 **화면 전체가 멈춥니다.** `knockedPins`를 고른 랙으로 필터해야 합니다.
- 연습 랙 재설치는 `onThrowResolved`가 아니라 `onReady`에서 해야 합니다.

이 넷은 코드에 주석으로도 남겨 두었습니다.

### 개발 환경 복원

```bash
npm install     # node_modules는 저장소에 없습니다
npm run dev     # http://localhost:5173/
npm test        # 294개 통과해야 정상
npm run typecheck
npm run build
```

온라인 대전까지 켜서 보려면 `.env.local`에 `VITE_SUPABASE_URL`·`VITE_SUPABASE_ANON_KEY`를
채워야 합니다 (파일은 git에 없습니다 — 위 "구현 현황" 상단 안내 참고).
